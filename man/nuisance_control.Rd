% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{nuisance_control}
\alias{nuisance_control}
\title{Control parameters for nuisance parameter estimation}
\usage{
nuisance_control(
  learners_trt = "SL.glm",
  learners_outcome = "SL.glm",
  learners_refit = NULL,
  outer_folds = 5L,
  inner_folds = 5L,
  epsilon = 1e-05,
  folds = NULL,
  seed = NULL
)
}
\arguments{
\item{learners_trt}{A character vector of \pkg{SuperLearner} libraries for estimating
the propensity scores. For longitudinal estimands the same library is used at every time point.}

\item{learners_outcome}{A character vector of \pkg{SuperLearner} libraries for
estimating the outcome regressions. For longitudinal estimands the same
library is used for every sequential regression.}

\item{learners_refit}{Optional vector of \pkg{SuperLearner} libraries for
sequential regressions in targeted longitudinal estimation.}

\item{outer_folds}{Number of folds in the outer cross-fitting loop.}

\item{inner_folds}{Number of folds for the inner \pkg{SuperLearner} cross-validation
within each outer cross-fitting fold.}

\item{epsilon}{Truncation bound. Estimated propensity scores are constrained to
\eqn{[\epsilon, 1 - \epsilon]}, and for \code{outcome_type = "binomial"} so are the
estimated conditional means.}

\item{folds}{Optional pre-constructed fold assignment, as returned by \code{\link[origami:make_folds]{origami::make_folds()}}.}

\item{seed}{Optional integer seed applied to the whole nuisance estimation step.}
}
\value{
A list of class \code{"automsm_nuisance_control"}.
}
\description{
Constructs the settings governing estimation of the nuisance parameters.
Pass the result to the \code{nuisance} argument of \code{\link[=cate]{cate()}}, \code{\link[=dose_response]{dose_response()}}, or \code{\link[=longitudinal_dose_response]{longitudinal_dose_response()}}.
}
\details{
To supply nuisance estimates computed elsewhere and bypass estimation entirely,
use the separate \code{nuisance_estimates} argument instead.
}
\examples{
# Defaults: 5-fold cross-fitting, main-terms GLM for both nuisances
nuisance_control()

# Ensemble with reproducible fold assignment
nuisance_control(
  learners_trt = c("SL.glm", "Sl.ranger", "SL.mean"),
  learners_outcome = c("SL.glm", "SL.ranger", "SL.mean"),
  outer_folds = 10,
  seed = 10016
)

}
\seealso{
\code{\link[=tmle_control]{tmle_control()}}, \code{\link[=bayes_control]{bayes_control()}}, \code{\link[=onestep_control]{onestep_control()}};
\code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner()}} for the learner libraries;
\code{\link[origami:make_folds]{origami::make_folds()}} for the \code{folds} format.

Other automsm control: 
\code{\link{bayes_control}()},
\code{\link{onestep_control}()},
\code{\link{tmle_control}()}
}
\concept{automsm control}
