% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{bayes_control}
\alias{bayes_control}
\title{Control parameters for the generalied Bayesian TMLE estimator}
\usage{
bayes_control(
  enabled = TRUE,
  draws = 1000,
  warmup = 1000,
  chains = 4L,
  prior = function(beta) sum(stats::dnorm(as.numeric(beta), 0, 1, log = TRUE)),
  fluctuation = c("auto", "linear", "logistic"),
  scale = NULL,
  acc_rate = 0.3,
  eps_max = NULL,
  min_ess = NULL,
  seed = NULL,
  labels = c("index", "terms")
)
}
\arguments{
\item{enabled}{Logical; whether to run the generalized Bayesian TMLE estimator.}

\item{draws}{Number of MCMC draws retained per chain.}

\item{warmup}{Number of initial adaption draws.}

\item{chains}{Number of MCMC chains.}

\item{prior}{A function taking the numeric vector of working-model coefficients \eqn{\beta}
(of length \code{p}) and returning a scalar log-density. Defaults to independent standard normals.}

\item{fluctuation}{whether to use a linear TMLE fluctuation model (\code{"linear"})
a logistic one (\code{"logistic"}), or pick based on the outcome type (\code{"auto"}; continuous outcomes default to
linear, and binary outcomes logistic). Defaults to \code{"auto"}.}

\item{scale}{Initial proposal covariance for the adaptive sampler. If \code{NULL} (the default),
it is derived from the estimated efficient influence function. Supply a scalar or
length-\eqn{p} vector of variances, or a \eqn{p \times p} covariance matrix to override the default.
Note that this is only for the initial proposal; the sampler adapts toward \code{acc_rate}
during warmup.}

\item{acc_rate}{Target acceptance rate for the adaptive sampler.}

\item{eps_max}{Largest permitted \eqn{\|\epsilon\|_\infty}. Proposals outside the
box are assigned zero density. If \code{NULL}, resolved from the clever-covariate magnitudes
so that the fluctuated covariate distribution cannot collapse into a single observation.}

\item{min_ess}{Minimum effective sample size of \eqn{Q_\epsilon}. Defaults to \code{2 * p}:
\eqn{E_{Q_\epsilon}[\ddot{L}_m]} cannot have rank \code{p} on fewer than \code{p}
support points.}

\item{seed}{Integer; optional random seed for MCMC algorithm}

\item{labels}{How to label posterior parameters: \code{"index"} to label by index, or \code{"terms"} to
use the terms from the working model formula}
}
\value{
A list of class \code{"automsm_bayes_control"}.
}
\description{
Constructs the settings governing the iterative targeting step,
in which a prior is placed on the fluctuation paramter \eqn{epsilon}
and combined with the loss function to yield a generalized posterior
for the working-model coefficients \eqn{\beta}.
Pass the result to the \code{bayes} argument of \code{\link[=cate]{cate()}}, \code{\link[=dose_response]{dose_response()}},
or \code{\link[=longitudinal_dose_response]{longitudinal_dose_response()}}. As a shorthand, those arguments also
accept a single logical: \code{bayes = FALSE} is equivalent to
\code{bayes = bayes_control(enabled = FALSE)}.
}
\details{
The generalized Bayesian estimator is available for the single-time-point estimands only.
It requires \code{tmle} to be enabled.
}
\seealso{
\code{\link[=tmle_control]{tmle_control()}}, \code{\link[=onestep_control]{onestep_control()}}

Other automsm control: 
\code{\link{nuisance_control}()},
\code{\link{onestep_control}()},
\code{\link{tmle_control}()}
}
\concept{automsm control}
